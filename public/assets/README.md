# 运行时素材

该目录只保存网页运行时直接访问的素材。

```text
maigo/   迷子图片、音频与字体
poison/  Poison 卡牌和锅素材
tl/      惨剧轮回公共、FS 与 BTX 素材
```

代码中的路径统一以 `/assets/...` 开头。新增或移动素材时需要同步更新源码或相应 `config.json`，并在生产构建后检查浏览器网络面板中没有 404。

仓库不再保留一份未被运行时引用的根目录 `asset/` 原始素材副本；需要追溯时可从 Git 历史恢复。

游戏图片、音频与字体的权利归各自权利人所有。公开部署或再分发前，请确认相应授权。
